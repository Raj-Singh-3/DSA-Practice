import java.util.Arrays;
import java.util.PriorityQueue;

public class abc {
    public int[] maxSubsequence(int[] nums, int k) {
        int arr[] = new int[k];
        PriorityQueue<Integer> arr1 = new PriorityQueue<>(k);
        
        for(int i = 0;i<k;i++)
        {
            
        }
        return arr;
    }
}
